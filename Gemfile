source "https://rubygems.org"
ruby "2.4.0"
gem 'sinatra', '1.1.0'
gem 'httparty'

# .env file hold app configuration and gets loaded (no need to embecd  
gem 'dotenv'  
gem 'rake'

# Debugging
gem 'pry'

# Testing
gem 'minitest'
